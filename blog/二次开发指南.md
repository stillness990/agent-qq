# 二次开发指南

## 新增命令

在 `command_router.py` 中添加路由：

```python
if text.startswith("/newcmd "):
    return await self._newcmd(text.removeprefix("/newcmd ").strip(), message.user_id)
```

## 新增 MCP 能力

目录：

```text
plugins/mcp/
```

基础接口：

```python
class McpConnector(Protocol):
    async def call(self, request: McpRequest) -> McpResponse:
        ...
```

未来可接入：

- MCP Filesystem
- MCP GitHub
- MCP Browser
- MCP PostgreSQL
- MCP MySQL

## 新增 RAG 能力

目录：

```text
plugins/rag/
```

基础接口：

```python
class RagRetriever(Protocol):
    async def search(self, query: RagQuery) -> list[RagResult]:
        ...
```

可扩展数据源：

- Markdown
- PDF
- TXT
- 企业知识库

## 新增 Agent

目录：

```text
agents/
```

基础接口：

```python
class BaseAgent(Protocol):
    async def run(self, context: AgentContext) -> AgentResult:
        ...
```

建议 Agent 类型：

- Planner Agent
- Coding Agent
- Search Agent
- Ops Agent
